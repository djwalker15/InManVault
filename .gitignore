# Editor / vault
.obsidian

# Dependencies (app/ has its own; this covers any stray root install)
node_modules/

# Env & secrets
.env
*.local

# Supabase local state
supabase/.temp/
supabase/.branches/
supabase/.env.local

# dev-stack.sh runtime artifacts
.dev-stack-*.log
*.local.bak
